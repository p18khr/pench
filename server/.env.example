# Example env file for the server
# Copy this to `.env` and fill with real values (do NOT commit secrets)

# Basic email credentials (required)
EMAIL_USER=gojungleeadventures@gmail.com
EMAIL_PASS=eypdjnmyrzffehne

# Optional: use a named service (e.g. gmail) instead of SMTP host
#EMAIL_SERVICE=gmail

# Optional SMTP settings (when EMAIL_HOST is present the host/port/secure settings are used)
#EMAIL_HOST=smtp.example.com
#EMAIL_PORT=587
#EMAIL_SECURE=false

# Server port (default 10000)
#PORT=10000
